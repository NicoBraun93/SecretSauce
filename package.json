{
  "name": "secret-sauce",
  "version": "1.0.0",
  "description": "Native macOS app to manage environment variables and secrets",
  "private": true,
  "scripts": {
    "package": "bash native/package-app.sh",
    "dev": "cd native && swift run"
  }
}
